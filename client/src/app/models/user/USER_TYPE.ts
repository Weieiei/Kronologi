/**
 * Using this as an enum for types of users.
 * It's better to use a number rather than a binary true/false value.
 * What if we wanna add employees? We'd just add a 3rd possible type.
 */
export const USER_TYPE = {

    CLIENT: 0,
    ADMIN: 1

}
