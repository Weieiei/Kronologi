import { CardSizeDirective } from './card-size.directive';

describe('CardSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new CardSizeDirective();
    expect(directive).toBeTruthy();
  });
});
